# Authorino Operator Bundle Configuration
# This file configures the Authorino operator bundle metadata

# CSV Version and Name
CSV_VERSION="1.2.4"
NAME="authorino-operator.v${CSV_VERSION}"

# Display name and description
DISPLAY_NAME="Authorino Operator"
METADATA_DESCRIPTION="Enables authentication and authorization for Gateways and applications in a Gateway API network."

# Channel configuration
CHANNEL="stable"

# Valid subscription
VALID_SUBSCRIPTION='["Red Hat Connectivity Link"]'

# Documentation URL
DOC_URL="https://docs.redhat.com/en/documentation/red_hat_connectivity_link"

# Note: Image pullspecs are managed separately in image-pullspecs.yaml
# Note: Upgrade path (replaces/skipRange) is managed in the file-based catalog repository
